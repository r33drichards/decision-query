# Keeps the pinned GitHub Actions (checkout, CodeQL, cpp-linter, ClusterFuzzLite) current.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
